spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/competition?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: root
logging:
  level:
    org.springframework.web: debug
# mybatis 配置
mybatis:
  mapper-locations: classpath:mapper/*.xml
